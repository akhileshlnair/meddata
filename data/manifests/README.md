# Manifests

Machine-readable inventory for the corpus build.

- `datasets.csv`: high-level dataset registry with access and usefulness metadata
- future files can track checksums, download state, and shard destinations
