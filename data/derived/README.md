# Derived Corpora

This directory stores cleaned, normalized, SFT-ready shards derived from the source datasets.

Current convention:

- one dataset per JSONL shard family
- each row should carry provenance fields when possible
- keep an accompanying manifest entry for every shard created
